[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests that hit a real external service (e.g. live Appwrite project)
addopts = -m "not integration"
